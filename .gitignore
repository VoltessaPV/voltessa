# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# FusionSolar browser automation screenshots (may contain real customer plant data)
/tmp/

# Build Outputs
.next/
out/
build
dist


# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem

# ml-forecasting/ generated training artifacts (exported dataset, trained ONNX
# models, manifest) - regenerated by running the exporter + train.py; the
# deployed model is the ONNX bytes already persisted into
# ForecastModelVersion (Postgres), not this directory.
ml-forecasting/data/
